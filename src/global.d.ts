/**
 * These type declarations are for the `window.navigator.userAgentData`
 * property which is still experimental and thus not typed on
 * `window.navigator` by default.
 */

/// <reference types="user-agent-data-types" />
